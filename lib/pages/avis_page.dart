import 'package:flutter/material.dart';

class AvisPage extends StatelessWidget {
  const AvisPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
  child: Text("Liste des avis"),
    );
  }
}

